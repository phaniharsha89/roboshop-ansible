


- name: Install MySQL Client
  ansible.builtin.yum:
    name:  mysql
    state: installed

- name: Load Schema
  ansible.builtin.shell: mysql --host mysql-dev.harshadevops.shop -uroot -pRoboshop@1 </app/schema/{{ component }}.sql




   